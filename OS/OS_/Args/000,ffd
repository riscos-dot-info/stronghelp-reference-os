OS_Args 0
#Parent OS_Args
#Wrap off
=>	R0 =	0
	R1 =	file handle or 0
<=	R0 =	<filing system=>fsys_ident>	(if R1 = 0)
	R2 =	file pointer			(if R1 \<> 0)
#Wrap on

This SWI returns the temporary filing system no, or file pointer.