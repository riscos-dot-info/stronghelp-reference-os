Module flags word
#Parent OS_Module_format
This is a single word which informs RISC OS of the capabilities of your module. The flags word should not be placed immediately after the offset to the flags word in the module header, incase the module header needs to be extended in the future.

#tab
/Bit/	/Meaning when set/
#line
0	Module is 32 bit ARM compatible
1	Module is used for early ROM startup (for RISC OS Select internal use only)
2-31	Reserved, must be 0
# 31	Another flags word follows this one
